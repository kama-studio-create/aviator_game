import {css} from "@emotion/react";

const sideBarStyles = css([{
  width: '100%',
  height: '100%',
  borderRadius: 16,
}])
const Sidebar = () => {
  return (
    <div css={sideBarStyles}>
      {/*<BetSlips/>*/}
    </div>
  )
}

export default Sidebar
